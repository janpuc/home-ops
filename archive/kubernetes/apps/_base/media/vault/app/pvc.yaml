---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: vault
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  volumeName: vault
  storageClassName: hetzner
