apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: grafana-secrets
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: grafana-secrets
    template:
      data:
        public_ip: "{{ .public_ip }}"
  dataFrom:
    - extract:
        key: grafana
