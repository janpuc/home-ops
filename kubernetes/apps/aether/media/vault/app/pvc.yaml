---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: vault
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Ti
  volumeName: vault
  storageClassName: hetzner-smb
