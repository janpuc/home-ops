---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mealie
spec:
  chartRef:
    kind: OCIRepository
    name: mealie
  interval: 1h
  values:
    controllers:
      mealie:
        containers:
          app:
            image:
              repository: ghcr.io/mealie-recipes/mealie
              tag: v3.17.0@sha256:d570139f1891346131d02f8e4d1853100ae4846474958ca6c68cfe1648058467
            env:
              PUID: 1000
              PGID: 1000
              TZ: Europe/Warsaw
              # ALLOW_SIGNUP: "false"
              # ALLOW_PASSWORD_LOGIN: "false"
              BASE_URL: https://recipes.janpuc.com
              DB_ENGINE: sqlite
              # OIDC_AUTH_ENABLED: "true"
              # OIDC_CONFIGURATION_URL: https://auth.janpuc.com/.well-known/openid-configuration
              # OIDC_CLIENT_ID: mealie
              # OIDC_USER_GROUP: users
              # OIDC_ADMIN_GROUP: admins
              # OIDC_AUTO_REDIRECT: "true"
              # OIDC_PROVIDER_NAME: Authelia
              # OIDC_REMEMBER_ME: "true"
            envFrom:
              - secretRef:
                  name: "{{ .Release.Name }}-secret"
            resources:
              requests:
                cpu: 10m
                memory: 256Mi
              limits:
                memory: 512Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: {drop: ["ALL"]}
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        ports:
          http:
            port: 9000
    route:
      app:
        hostnames:
          - "recipes.janpuc.com"
          - "{{ .Release.Name }}.janpuc.com"
        parentRefs:
          - name: envoy-internal
            namespace: network
    persistence:
      config:
        type: emptyDir
        globalMounts:
          - path: /app/data
