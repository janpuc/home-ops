---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: minecraft
spec:
  chartRef:
    kind: OCIRepository
    name: minecraft
  interval: 1h
  values:
    deploymentAnnotations:
      secret.reloader.stakater.com/reload: &secret minecraft
    image:
      repository: ghcr.io/itzg/minecraft-server
      tag: 2026.4.0-java21
    resources:
      requests:
        cpu: 100m
        memory: 512Mi
      limits:
        memory: 4Gi
    securityContext:
      runAsUser: 1000
      fsGroup: 1000
    livenessProbe:
      initialDelaySeconds: 0
    readinessProbe:
      initialDelaySeconds: 0
    startupProbe:
      enabled: true
    extraEnv:
      TZ: "Europe/Warsaw"
      UID: 1000
      GID: 1000
      MEMORY: ""
    persistence:
      dataDir:
        enabled: true
        existingClaim: "${APP}"
    serviceAnnotations:
      lbipam.cilium.io/ips: "192.168.69.61"
    minecraftServer:
      eula: true
      version: 1.21.1
      type: AUTO_CURSEFORGE
      difficulty: normal
      pvp: true
      motd: "Socialist Utopia"
      worldSaveName: "Socialist Utopia 2"
      viewDistance: 12
      forceReDownload: true
      memory: ""
      jvmXXOpts: "-XX:MaxRAMPercentage=70"
      rcon:
        enabled: true
        serviceType: ClusterIP
      serviceType: LoadBalancer
      extraPorts:
        - name: map
          containerPort: 8100
          protocol: TCP
          service:
            enabled: true
            embedded: false
            type: ClusterIP
            port: 8100
        - name: metrics
          containerPort: 25585
          protocol: TCP
          service:
            enabled: true
            embedded: false
            type: ClusterIP
            port: 25585
      autoCurseForge:
        pageUrl: "https://www.curseforge.com/minecraft/modpacks/create-ultimate-selection-2"
  valuesFrom:
    - kind: Secret
      name: *secret
      valuesKey: RCON_PASSWORD
      targetPath: minecraftServer.rcon.password
    - kind: Secret
      name: *secret
      valuesKey: CF_API_KEY
      targetPath: minecraftServer.autoCurseForge.apiKey.key
