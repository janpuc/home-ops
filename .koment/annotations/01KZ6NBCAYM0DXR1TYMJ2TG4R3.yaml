# yaml-language-server: $schema=https://raw.githubusercontent.com/koment-dev/koment/main/schema/v1alpha/annotation.schema.json
apiVersion: koment.dev/v1alpha
kind: Annotation
metadata:
  id: 01KZ6NBCAYM0DXR1TYMJ2TG4R3
  created: "2026-08-04T00:00:00Z"
spec:
  target:
    file: .github/workflows/koment-pages.yaml
  type: gotcha
  title: koment version input must move with the mise config pin
  body: |-
    The koment version is pinned in TWO places and they must be bumped together:
    this `version` input and `"github:koment-dev/koment"` in .mise/config.toml.
    Renovate's mise manager tracks the config.toml pin and its action manager
    tracks the SHA above, but nothing tracks this input value — there is no
    customManager configured for workflow inputs in .renovaterc.json5.

    Letting them drift means CI runs `koment check` on a different build than
    the one developers run locally, so a check can pass on a laptop and fail
    here for reasons nobody can reproduce. If the two ever disagree,
    .mise/config.toml is the source of truth.
  anchor:
    scope: excerpt
    excerpt: |2-
            - name: Setup Koment
              uses: koment-dev/koment@ffd1070fd22b52fb42ed51dc5572ee8d08204e41 # v2.0.1
              with:
                version: 2.0.1
    before: |2
              with:
                persist-credentials: false
    after: |2-

            - name: Check Annotations
              run: |
  author:
    name: claude-code
    kind: agent
    source: session
status:
  lastSeenLine: 28
  resolution: ok
  resolvedAt: "2026-08-06T10:50:42Z"
  resolvedCommit: 0d62298c8abd458094f7a20bf0568419574e7747
