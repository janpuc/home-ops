# yaml-language-server: $schema=https://raw.githubusercontent.com/koment-dev/koment/main/schema/v1alpha/annotation.schema.json
apiVersion: koment.dev/v1alpha
kind: Annotation
metadata:
  id: 01KZ6MDT18PH5S1YRXZYTF8P7Q
  created: "2026-08-04T00:00:00Z"
spec:
  target:
    file: kubernetes/apps/ai/hermes/app/configmap.yaml
  type: why
  title: kubectl MCP include list trims families this cluster does not run
  body: |-
    This include list is a token-budget decision, not a capability one: 275
    tools upstream (~44.9k tokens) down to 95 (~16.8k). What was dropped is
    whole families this cluster does not run — kind, vind, kubevirt, istio,
    capi, keda, argo-rollouts, flagger, velero backup/restore, multi-cluster —
    plus the roughly 35 helm_* tools, since Flux owns Helm here.

    Adding a tool back is fine; adding a family back costs real tokens on every
    request.
  anchor:
    scope: excerpt
    excerpt: |2-
              tools:
                include:
                  - kubectl-get_pods
    before: |2-
              url: http://litellm.ai:4000/mcp/kubectl/
              headers:
                Authorization: "Bearer $${LITELLM_API_KEY}"
    after: |2-
                  - kubectl-get_nodes
                  - kubectl-get_nodes_summary
                  - kubectl-get_node_metrics
  author:
    name: claude-code
    kind: agent
    source: session
  git:
    commit: d66c205d93c0bde7fcbc5bacceeaed825f9ad72f
    path: kubernetes/apps/ai/hermes/app/configmap.yaml
    line: 240
status:
  lastSeenLine: 240
